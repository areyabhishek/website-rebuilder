---
// Homepage
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Analytics & Robust Event Tracking | Mixpanel</title>
  <meta name="description" content="Analytics that drive decisions. Mixpanel provides powerful product analytics to help teams understand user behavior and optimize their products.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
</head>
<body>
  <header class="header">
    <div class="container">
      <nav class="nav">
        <a href="/" class="logo">Mixpanel</a>
        <ul class="nav-links">
          <li><a href="/">Home</a></li>
          <li><a href="/partners">Partners</a></li>
          <li><a href="/platform/product-analytics">Product Analytics</a></li>
          <li><a href="/platform/web-analytics">Web Analytics</a></li>
          <li><a href="/platform/mobile-analytics">Mobile Analytics</a></li>
          <li><a href="/releases/data-warehouse-connectors">Data Warehouse Connectors</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <div class="announcement">
            🎉 News - DoubleLoop joins Mixpanel! <a href="https://mixpanel.com/blog/mixpanel-acquires-doubleloop/" class="announcement-link">See what this means</a>
          </div>
          <h1 class="hero-title">Analytics that drive decisions</h1>
          <p class="hero-description">Make your move with powerful product analytics that help teams understand user behavior and optimize their products.</p>
          <div class="hero-actions">
            <a href="https://mixpanel.com/register" class="btn btn-primary">Get Started Free</a>
            <a href="https://mixpanel.com/contact-us/demo-request" class="btn btn-secondary">Book a Demo</a>
          </div>
        </div>
      </div>
    </section>

    <section class="metrics">
      <div class="container">
        <div class="metrics-grid">
          <div class="metric-card">
            <div class="metric-number">15%</div>
            <div class="metric-label">in accounts</div>
          </div>
          <div class="metric-card">
            <div class="metric-number">5%</div>
            <div class="metric-label">in upgrades</div>
          </div>
          <div class="metric-card">
            <div class="metric-number">10%</div>
            <div class="metric-label">in first-time conversions</div>
          </div>
        </div>
        <a href="https://mixpanel.com/customers/docusign-accelerates-growth/" class="case-study-link">Read Case Study</a>
      </div>
    </section>

    <section class="support">
      <div class="container">
        <h2 class="section-title">Support for every step of the journey</h2>
        <p class="section-description">Whether you're moving fast on your own or need expert guidance, we're here to help you succeed.</p>
        <div class="platform-links">
          <a href="https://mixpanel.com/platform/product-analytics" class="platform-link">Product Analytics</a>
          <a href="https://mixpanel.com/platform/web-analytics" class="platform-link">Web Analytics</a>
        </div>
      </div>
    </section>

    <section class="cta">
      <div class="container">
        <div class="cta-content">
          <h2 class="cta-title">Mixpanel is for teams that move. Make your move.</h2>
          <div class="cta-actions">
            <a href="https://mixpanel.com/register/" class="btn btn-primary">Get Started Free</a>
            <a href="https://mixpanel.com/contact-us/demo-request/" class="btn btn-secondary">Book a Demo</a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <style>
    :root {
      --brand: #6366f1;
      --brand-alt: #8b5cf6;
      --bg: #ffffff;
      --surface: #f8fafc;
      --text: #1e293b;
      --muted: #64748b;
      
      --font-heading: 'Inter', sans-serif;
      --font-body: 'Inter', sans-serif;
      
      --space-0: 0px;
      --space-1: 4px;
      --space-2: 8px;
      --space-3: 12px;
      --space-4: 16px;
      --space-5: 20px;
      --space-6: 24px;
      --space-8: 32px;
      --space-10: 40px;
      --space-12: 48px;
      --space-16: 64px;
      --space-20: 80px;
      --space-24: 96px;
      
      --radius-sm: 4px;
      --radius-md: 8px;
      --radius-lg: 12px;
      
      --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
      --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-body);
      background-color: var(--bg);
      color: var(--text);
      line-height: 1.6;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 var(--space-4);
    }

    .header {
      border-bottom: 1px solid var(--surface);
      padding: var(--space-4) 0;
    }

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo {
      font-family: var(--font-heading);
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--brand);
      text-decoration: none;
    }

    .nav-links {
      display: flex;
      list-style: none;
      gap: var(--space-8);
    }

    .nav-links a {
      color: var(--text);
      text-decoration: none;
      font-weight: 500;
      transition: color 0.2s;
    }

    .nav-links a:hover {
      color: var(--brand);
    }

    .hero {
      padding: var(--space-24) 0;
      text-align: center;
    }

    .announcement {
      background-color: var(--surface);
      padding: var(--space-3) var(--space-6);
      border-radius: var(--radius-lg);
      margin-bottom: var(--space-8);
      display: inline-block;
      font-size: 0.9rem;
    }

    .announcement-link {
      color: var(--brand);
      text-decoration: none;
    }

    .hero-title {
      font-family: var(--font-heading);
      font-size: 3.5rem;
      font-weight: 700;
      margin-bottom: var(--space-6);
      color: var(--text);
    }

    .hero-description {
      font-size: 1.25rem;
      color: var(--muted);
      margin-bottom: var(--space-10);
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .hero-actions {
      display: flex;
      gap: var(--space-4);
      justify-content: center;
    }

    .btn {
      padding: var(--space-4) var(--space-8);
      border-radius: var(--radius-md);
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s;
      display: inline-block;
    }

    .btn-primary {
      background-color: var(--brand);
      color: white;
      border: none;
    }

    .btn-primary:hover {
      background-color: var(--brand-alt);
      transform: translateY(-1px);
      box-shadow: var(--shadow-md);
    }

    .btn-secondary {
      background-color: transparent;
      color: var(--text);
      border: 2px solid var(--surface);
    }

    .btn-secondary:hover {
      background-color: var(--surface);
      transform: translateY(-1px);
      box-shadow: var(--shadow-sm);
    }

    .metrics {
      padding: var(--space-20) 0;
      background-color: var(--surface);
    }

    .metrics-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--space-8);
      margin-bottom: var(--space-12);
    }

    .metric-card {
      text-align: center;
      background-color: var(--bg);
      padding: var(--space-10);
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-sm);
    }

    .metric-number {
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--brand);
      margin-bottom: var(--space-2);
    }

    .metric-label {
      color: var(--muted);
      font-weight: 500;
    }

    .case-study-link {
      color: var(--brand);
      text-decoration: none;
      font-weight: 600;
      display: block;
      text-align: center;
    }

    .support {
      padding: var(--space-20) 0;
      text-align: center;
    }

    .section-title {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: var(--space-6);
    }

    .section-description {
      font-size: 1.25rem;
      color: var(--muted);
      margin-bottom: var(--space-10);
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .platform-links {
      display: flex;
      gap: var(--space-6);
      justify-content: center;
    }

    .platform-link {
      color: var(--brand);
      text-decoration: none;
      font-weight: 600;
      padding: var(--space-3) var(--space-6);
      border: 2px solid var(--brand);
      border-radius: var(--radius-md);
      transition: all 0.2s;
    }

    .platform-link:hover {
      background-color: var(--brand);
      color: white;
    }

    .cta {
      padding: var(--space-24) 0;
      background-color: var(--surface);
      text-align: center;
    }

    .cta-title {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: var(--space-10);
    }

    .cta-actions {
      display: flex;
      gap: var(--space-4);
      justify-content: center;
    }

    @media (max-width: 768px) {
      .nav-links {
        display: none;
      }

      .hero-title {
        font-size: 2.5rem;
      }

      .hero-actions,
      .cta-actions {
        flex-direction: column;
        align-items: center;
      }

      .platform-links {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</body>
</html>